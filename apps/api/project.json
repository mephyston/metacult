{
    "name": "api",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "apps/api",
    "targets": {
        "serve": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun start",
                "cwd": "apps/api"
            }
        }
    }
}