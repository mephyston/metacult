name: CI/CD Pipeline
run-name: "${{ github.event_name == 'workflow_dispatch' && format('🚀 Prod Release: {0} ({1})', inputs.releaseType, inputs.preid) || format('🚧 Staging: {0}', github.event.head_commit.message) }}"

on:
  push:
    branches:
      - develop
  workflow_dispatch:
    inputs:
      releaseType:
        description: 'Type of release'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
          - prerelease
      preid:
        description: 'Prerelease Identifier (only for prerelease)'
        required: false
        default: 'none'
        type: choice
        options:
          - none
          - alpha
          - beta
          - rc

permissions:
  contents: write
  packages: write
  actions: read

env:
  NX_HEAD: HEAD
  NX_BASE: HEAD~1

jobs:
  # ------------------------------------------------------------------
  # JOB 1 (Part A): STAGING CHECKS
  # Trigger: Push on develop
  # Goal: Lint & Test only affected projects
  # ------------------------------------------------------------------
  staging-checks:
    name: 🧪 Staging Checks
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Cache Playwright Browsers
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json', '**/yarn.lock', '**/pnpm-lock.yaml', '**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      # - name: Install Playwright Browsers
      #   run: bunx playwright install --with-deps

      - name: Lint Affected
        run: bun run nx affected -t lint --parallel=3 --verbose

      - name: Test Affected
        run: bun run nx affected -t test --parallel=3 --exclude=e2e --verbose

  # ------------------------------------------------------------------
  # JOB 1 (Part B): STAGING BUILD & DEPLOY
  # Trigger: Push on develop (follows checks)
  # Goal: Build -> Push (staging+sha) -> Deploy Railway Staging
  # ------------------------------------------------------------------
  staging-deploy:
    name: 'Staging Deploy - ${{ matrix.app }}'
    needs: staging-checks
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        app: [api, webapp, website, worker]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lowercase Repo Owner
        id: get-owner
        run: echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/${{ matrix.app }}/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ steps.get-owner.outputs.owner }}/metacult-${{ matrix.app }}:staging
            ghcr.io/${{ steps.get-owner.outputs.owner }}/metacult-${{ matrix.app }}:sha-${{ github.sha }}
          build-args: |
            GIT_SHA=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway Staging
        # Using 'redeploy' to trigger a pull of the new image from GHCR
        # Service name is just the app name (lowercase)
        # Note: Environment is determined by RAILWAY_TOKEN (staging token = staging env)
        run: |
          n=0
          until [ "$n" -ge 3 ]
          do
             railway redeploy --service ${{ matrix.app }} --yes && break
             n=$((n+1))
             echo "⚠️ Deploy failed. Retrying in 10s ($n/3)..."
             sleep 10
          done
          if [ "$n" -ge 3 ]; then
            echo "❌ Failed to deploy after 3 attempts."
            exit 1
          fi
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN_STAGING }}

  # ------------------------------------------------------------------
  # JOB 2: PROD RELEASE & DEPLOY
  # Trigger: Manual workflow_dispatch
  # Goal: Version (Nx) -> Promote Image (Staging->Prod) -> Deploy Railway Prod
  # ------------------------------------------------------------------
  prod-release:
    name: 🚀 Prod Release & Deploy
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Generate Release Name
        id: release-name
        run: |
          ADJECTIVES=("Cosmic" "Shiny" "Epic" "Fast" "Silent" "Brave" "Witty" "Mighty" "Clever" "Wild")
          ANIMALS=("Bear" "Wolf" "Eagle" "Tiger" "Shark" "Fox" "Lion" "Hawk" "Owl" "Panda")
          RANDOM_ADJ=${ADJECTIVES[$RANDOM % ${#ADJECTIVES[@]}]}
          RANDOM_ANI=${ANIMALS[$RANDOM % ${#ANIMALS[@]}]}
          RELEASE_NAME="$RANDOM_ADJ $RANDOM_ANI"
          echo "RELEASE_NAME=$RELEASE_NAME" >> $GITHUB_OUTPUT
          echo "Generated Release Name: $RELEASE_NAME"

      - name: Nx Release (Version & Changelog)
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HUSKY: 0
        run: |
          # ⚠️ Disable Husky hooks by removing the directory
          # This prevents tests/lint-staged from running in this job (missing env vars)
          rm -rf .husky

          if [ "${{ inputs.preid }}" == "none" ]; then
             bun run nx release ${{ inputs.releaseType }} --skip-publish
          else
             bun run nx release ${{ inputs.releaseType }} --preid=${{ inputs.preid }} --skip-publish
          fi

      - name: Get Released Version
        id: get-version
        run: echo "VERSION=$(git describe --tags --abbrev=0)" >> $GITHUB_OUTPUT

      - name: Lowercase Repo Owner
        id: get-owner
        run: echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Initialize Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Promote Images & Deploy Prod
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN_PRODUCTION }}
        run: |
          VERSION=${{ steps.get-version.outputs.VERSION }}
          OWNER=${{ steps.get-owner.outputs.owner }}
          APPS=("api" "webapp" "website" "worker")

          echo "🔥 Promoting images for version: $VERSION"

          echo "🔥 Promoting images for version: $VERSION"

          # 1. PROMOTION PHASE
          # We promote ALL images first. If any fails, the script exits, and no deployments occur.
          for APP in "${APPS[@]}"; do
            SHA_TAG="ghcr.io/$OWNER/metacult-$APP:sha-${{ github.sha }}"
            STAGING_TAG="ghcr.io/$OWNER/metacult-$APP:staging"
            
            SOURCE_IMAGE="$SHA_TAG"
            if ! docker buildx imagetools inspect "$SOURCE_IMAGE" &>/dev/null; then
               echo "⚠️  Image $SHA_TAG not found. Falling back to $STAGING_TAG"
               SOURCE_IMAGE="$STAGING_TAG"
            fi
            
            TARGET_LATEST="ghcr.io/$OWNER/metacult-$APP:latest"
            TARGET_VERSION="ghcr.io/$OWNER/metacult-$APP:$VERSION"
            
            echo "---------------------------------------------------"
            echo "📦 Promoting App: $APP"
            echo "   Source: $SOURCE_IMAGE"
            echo "   Targets: $TARGET_LATEST, $TARGET_VERSION"
            
            docker buildx imagetools create -t $TARGET_LATEST -t $TARGET_VERSION $SOURCE_IMAGE
          done

          echo "✅ All images promoted successfully. Starting deployment..."

          # Retry helper function
          function retry_cmd {
            local n=1
            local max=3
            local delay=10
            while true; do
              "$@" && break || {
                if [[ $n -lt $max ]]; then
                  ((n++))
                  echo "⚠️ Command failed. Retrying in ${delay}s ($n/$max)..."
                  sleep $delay;
                else
                  echo "❌ Command failed after $n attempts."
                  return 1
                fi
              }
            done
          }

          # 2. DEPLOYMENT PHASE
          # Only runs if all promotions succeeded.
          for APP in "${APPS[@]}"; do
            echo "🚀 Deploying $APP to Production..."
            echo "💉 Injecting Version $VERSION into Railway ($APP)..."

            # On injecte la version. Railway redémarrera automatiquement le service.
            retry_cmd railway variables --set "APP_VERSION=$VERSION" --service $APP --environment production

            # Cas spécifique Nuxt pour le frontend
            if [ "$APP" == "webapp" ]; then
                retry_cmd railway variables --set "NUXT_PUBLIC_APP_VERSION=$VERSION" --service $APP --environment production
            fi
          done

          echo "✅ All services promoted and deployed to Production!"
